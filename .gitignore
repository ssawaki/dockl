.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Per-developer Claude Code settings (approved permissions and the like). The shared
# project settings next to it, .claude/settings.json, is meant to be committed.
.claude/settings.local.json
